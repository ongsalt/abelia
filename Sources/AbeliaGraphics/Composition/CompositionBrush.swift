public enum CompositionBrush {
  case solid(Color)
  // case texture(Texture, crop, ninegrid) mapped to Brush.texture
  // case gradient(Color) mapped to Brush.texture
  // case backdropEffect(Effect) mapped to Brush.texture
}
